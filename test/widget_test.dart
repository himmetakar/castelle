// Castelle widget testleri
// Bu dosya ilerleyen adımlarda güncellenecektir.

import 'package:flutter_test/flutter_test.dart';

void main() {
  testWidgets('Castelle app smoke test', (WidgetTester tester) async {
    // Placeholder test - Firebase bağımlılığı nedeniyle
    // gerçek widget testleri integration test olarak yazılacak
    expect(1 + 1, equals(2));
  });
}
